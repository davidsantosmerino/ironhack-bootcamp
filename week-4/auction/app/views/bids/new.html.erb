<%= form_for [@user, @product , @bid] do |f| %>
  <div>
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>
  <%= hidden_field_tag "bid[user_id]", @user.id%>


  <%= f.submit "bid" %>
<% end %>
